# Optional RSA encryption keys (PEM format)
# Generate with: openssl genrsa -out private.pem 2048
#                openssl rsa -in private.pem -pubout -out public.pem

RSA_PUBLIC_KEY=""
RSA_PRIVATE_KEY=""
